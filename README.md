# CVFlash👨‍🎓

## 👋Project Overview
CVFlash is a full-stack resume builder web application that enables users to create professional resumes using pre-designed templates with real-time preview functionality.
The application is built using MongoDB, Express.js, React.js, and Node.js and follows a clean client-server architecture.

## Features💖
- Dynamic Resume Builder with Structured Sections
- Real-Time Resume Preview
- Multiple Pre-Designed Resume Templates
- Controlled Form Handling for User Input
- Save & Update Resume Data
- Download Resume as PDF
- Secure Backend API Integration
- Organized Data Storage using MongoDB
- Fully Responsive User Interface

## Architecture⚙️
### 🥇Frontend 
- Built using ReactJS and styled with TailwindCSS.
- It provides the user interface and handles all user interactions.
### 🥈Backend  
- Developed using Node.js and Express.js.
- It manages business logic, authentication, and API handling.
### 🥉Database 
- The application uses MongoDB as its primary database.

## Installation 📝
1️⃣ Clone the repository : \
`git clone https://github.com/Ajinkya-Anap/CVFlash.git` \
\
2️⃣ Install the dependencies for the backend : \
`cd CVFlash/backend && npm install` \
\
3️⃣ Install the dependencies for the frontend : \
`cd ../frontend && npm install` \
\
4️⃣ Configure the environment variables by creating a `.env` file based on the provided templates in the `backend` and `frontend` directories. \
\
5️⃣ Start the backend server : \
`npm start` \
\
6️⃣ Start the frontend development server : \
`npm start` \
\
7️⃣ Access the application at `http://localhost:3000`.

## Contributing🎈
Contributions are welcome! \
If you would like to contribute to TaskPilot, please follow these steps: \
\
1️⃣ Fork the Repository. \
\
2️⃣ Clone Your Fork. 
``` bash 
git clone https://github.com/Ajinkya-Anap/CVFlash.git
cd CVFlash
```
\
3️⃣ Create a New Branch. \
`git checkout -b feature/your-feature-name` \
\
4️⃣ Make Changes & Commit. \
`git commit -m "Add your meaningful commit message here"` \
\
5️⃣ Push to Your Fork. \
`git push origin feature/your-feature-name` \
\
6️⃣ Open a Pull Request. 
